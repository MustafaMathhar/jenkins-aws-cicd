pipeline {
    agent { label 'default' }
    stages {
        stage('Tests') {
            dir('src') {
                sh 'pwd'
            }
        }
        stage('Smoke Test') {
        }
    }
}
