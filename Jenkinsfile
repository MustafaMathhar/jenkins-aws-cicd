pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Hello..'
                echo 'ECHO ECHO'
                sh 'docker build'
            }
        }
    }
}
