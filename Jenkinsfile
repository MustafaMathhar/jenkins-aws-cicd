pipeline {
agent {label 'default'}
    stages {
    stage('Hello') {
            steps {
                echo 'Hello..'
                echo 'ECHO ECHO'
                sh 'docker ps'
            }
    }
    }
}
